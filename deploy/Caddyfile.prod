{
  email {$ACME_EMAIL}
}

{$APP_DOMAIN} {
  encode gzip zstd

  request_body {
    max_size 1100MB
  }

  @blocked_paths {
    path /metrics /metrics/*
  }
  respond @blocked_paths 403

  reverse_proxy api:8000
}
